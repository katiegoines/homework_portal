    <h3>Recently Submitted</h3>
    <% @assignments.where(submit:"Yes").order('updated_at DESC').take(2).each do |a| %>
        <%= render 'layouts/partials/card', :a => a %>
    <% end %>
    <div class="clear"></div>
    <p class="lead">
        <%= link_to "See all submitted reports", assignments_path, class:'btn' %>
        <%= link_to "Edit Profile", edit_user_path, class:'btn' %>
    </p>
